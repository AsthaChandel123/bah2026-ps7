# exopipe -- optional dependencies, grouped by extra.
# Install everything with:  pip install -r requirements-optional.txt
# (or install just one group via the pyproject extras, e.g. `pip install -e .[science]`)

# --- science: astronomy / transit-modelling stack -------------------------- #
astropy>=5.1
lightkurve>=2.4
transitleastsquares>=1.0.31
wotan>=1.10
batman-package>=2.4.9
emcee>=3.1
dynesty>=2.1
astroquery>=0.4.6

# --- ml: classical machine learning ---------------------------------------- #
scikit-learn>=1.2
xgboost>=1.7
lightgbm>=3.3

# --- dl: deep learning ----------------------------------------------------- #
torch>=2.0

# --- perf: performance, parallelism, columnar I/O -------------------------- #
numba>=0.57
joblib>=1.2
pyarrow>=12.0
zarr>=2.14
bottleneck>=1.3
hnswlib>=0.7

# --- app: interactive dashboard & rich plotting ---------------------------- #
streamlit>=1.25
plotly>=5.14

# --- config: YAML configuration loading ------------------------------------ #
pyyaml>=6.0

# --- dev: testing & linting ------------------------------------------------ #
pytest>=7.0
ruff>=0.1
